# Roadmap

- Add Alembic migration generation CLI.
- Add richer fuzzy duplicate scoring configuration per language.
- Add side-by-side visual diff for version history.
- Add optional encrypted local backup bundle.
- Add plugin API for custom importers and validators.
