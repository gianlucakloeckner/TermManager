# Import / Export

Supported formats:
- JSON
- CSV
- Excel (`.xlsx`, `.xls`)

Data includes:
- term fields
- chapter IDs
- synonyms JSON payload
- annotations plain-text field

Use top bar buttons:
- `Import` to load a file
- `Export` to write current data
